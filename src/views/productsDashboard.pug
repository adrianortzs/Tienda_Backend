include ./htmlHead.pug
        include ./navBarDashboard.pug
        main
            h1 TIENDA DE ROPA
            section(class="products") 
                each product in products
                    div(class="product")
                        img(src=`${product.image}` alt=`${product.name}`)
                        section(class="important-info")
                            h3 #{product.name}
                            p #{product.price}€
                        a(href=`/dashboard/${product._id}`) Ver detalle